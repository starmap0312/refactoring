# - lazy class: if a class you create costs a lot of effort to maintain and understand, in other
#   words, a class is not doing enough worth existing should be eliminated
# - lazy class: use inline class to move all its features into another class and then delete it
# - lazy subclass: if a subclass is not doing enough, try to use collapse hierarchy
